# 前端扫描报告：敦煌金 AI 设计平台

扫描路径：`/mnt/d/DunHuangGold-Design-AI-main`
扫描时间：2026-07-28
扫描方式：静态代码审查（未执行 build / lint）

---

## 1. 技术栈版本

### 现状

| 维度 | 版本 | 来源 |
|------|------|------|
| Next.js | **15.1.0** | `package.json` dependencies |
| React | **19.2.3** | `package.json` |
| TypeScript | **5.9.3** | `package.json` devDependencies |
| Tailwind CSS | **v4** | `tailwindcss: ^4` + `@tailwindcss/postcss: ^4` |
| PostCSS 配置 | v4（仅 `@tailwindcss/postcss`） | `postcss.config.mjs` |
| shadcn/ui | 风格 **new-york** + base **neutral** + RSC 模式 | `components.json` |
| next-themes | **0.4.6 已装**，仅在 `sonner.tsx` 使用 | `package.json` |
| shadcn CLI | `shadcn: latest` (devDeps) | `package.json` |

### 证据
- `package.json` 第 67 行：`"next": "15.1.0"`
- `package.json` 第 70/72 行：`"react": "19.2.3"`、`"react-dom": "19.2.3"`
- `package.json` 第 84 行：`"@tailwindcss/postcss": "^4"`
- `package.json` 第 68 行：`"next-themes": "^0.4.6"`
- `components.json` 全文：`style: "new-york"`, `rsc: true`, `cssVariables: true`, `iconLibrary: "lucide"`
- `postcss.config.mjs` 全文（7 行）：仅声明 `@tailwindcss/postcss`

### 文档失真 ⚠️
- `AGENTS.md` 头部称"Next.js 16"和"React 19"——版本号对，但 Next 实际是 **15.1.0** 而非 16（**AGENTS.md 与 package.json 不一致**）。`eslint-config-next: 16.1.1` 也是一个版本错配（Next 15 应当配 15.x）。

### 缺口评级
- **中等**：版本一致性 / 锁文件质量。`pnpm-lock.yaml` 442KB 仍存在，但 docs 与 manifest 错位是项目交付的隐患。

---

## 2. UI 组件库

### 现状（统计）

| 目录 | 数量 | 备注 |
|------|------|------|
| `src/components/ui/` | **40 个 .tsx** | 33 个 shadcn 基础 + 7 个自定义（FloatingToolbar / GlobalEffects / ImageLoader / LoadingState / PromptInput / SingleImageUploadBox / 已有 loading） |
| `src/components/workspace/` | **19 个 .tsx** + 1 个 sub-components 目录 | 17 个 AI 功能 + WorkspacePanel + TryOnEffect + ImageWorkspace + 4 个 sub |
| `src/components/admin/` | **12 个 .tsx** | 后台 API/ComfyUI/规则/用户/提示词管理 |
| `src/components/layout/` | **2 个 .tsx** | Header, Sidebar |
| `src/components/profile/` | **3 个 .tsx** | ProfileEnhance, HistoryActions, RechargeModal |
| `src/components/app/` | **1 个 .tsx** | AppInitializer |

### shadcn 已实现组件（33 个）
`alert, avatar, badge, button, card, checkbox, collapsible, command, dialog, empty, field, form, input, item, kbd, label, loading, pagination, popover, progress, radio-group, select, separator, sheet, skeleton, sonner, spinner, switch, table, tabs, textarea, toast, toggle, tooltip`

### workspace AI 功能组件（17 个）
`Text2Image / AIDialog / ReliefDesign / Image3D / Dialog2D3D / ProductRefine / MultiImage / OneClickDesign / MultiView / SketchRealistic / FreeCreation / Text2Video / Image2Video / RemoveBackground / Upscale / RemoveWatermark / TryOnEffect`（均与 AGENTS.md 描述一致）

### 复用度
- **WorkspacePanel** 是中心调度器：使用 `React.lazy + Suspense` 懒加载所有 17 个功能面板（每个一个 import），按 `activePanel` key 索引。**这是项目最大的复用架构成果。**
- `sub-components/` 目录（HistorySidebar / ImageToolbar / PreviewModal / index）—— 表明已开始抽离工作台共用片段，但仅 3 个文件，**复用度仍处早期**。

### 证据
- `WorkspacePanel.tsx:8-24` 17 个 `lazy(() => import(...))`
- `search_files` 实测：`ui=40、workspace=19、admin=12、layout=2`

### 缺口评级
- **小**（功能层覆盖完整）
- **中**（DRY 仍可优化：表单/上传框/工具栏等模式可继续下沉到 ui 层）

---

## 3. 主题系统

### 现状
- **Tailwind v4 语法**（`@import 'tailwindcss'; @custom-variant dark (...); @theme inline { ... }`）—— 100% 符合 v4 新约定。
- `src/app/globals.css` **34,735 字节 / 1,224 行**（含 1,072 行业务样式）。
- 主题切换**强制深色**：`<html lang="zh-CN" className="dark">` 在 `layout.tsx:59` 硬编码。**不支持明色模式**（`AGENTS.md` 描述"深色背景系统"与此一致，但与 shadcn 默认 light/dark 双模脱节）。
- **敦煌金主题**已系统化：
  - 6 阶金色变量（`--gold` 至 `--gold-muted`），主色 `#D4AF37`
  - 完整背景阶（`--bg-primary` → `--bg-elevated`）
  - 4 阶文字阶（`primary/secondary/muted/dim`）
  - 4 阶功能色（success=翠玉绿, error=朱砂红, warning=琥珀黄, info=青花蓝）
  - 6 阶阴影 + 4 阶过渡 + 12 个 keyframes（金脉冲 / 流光 / 金沙 / 边框流光 / 浮游 / 旋转 / 骨架屏 / 进度条 / 弹跳 / 脉动 / 缩放 / 淡入淡出等）
  - SVG 藻井纹样背景（data-URI，0.04 透明度）
  - 6 个语义工具类（`.glow-selected / .glow-btn-primary / .glow-shimmer / .glow-icon / .glow-loading / .glow-input`）

### 证据
- `globals.css:1-4`：`@import 'tailwindcss'` + `@custom-variant dark` + `@theme inline`
- `globals.css:57-152`：`:root` 完整变量系统
- `layout.tsx:59`：`className="dark"` 硬编码
- `viewport.themeColor: '#C8A45C'`（`layout.tsx:48`）

### 缺口评级
- **中**：主题系统本身**完整且有品牌识别度**，但
  1. next-themes 装了却**只在 sonner 中使用**（`grep useTheme` 仅 1 个文件），没有 ThemeProvider 在 layout 中挂载；
  2. `<html>` 硬编码 `dark`，**无 light 主题**，对未来国际化/可访问性是限制；
  3. 1,224 行 globals.css 偏胖，建议拆分为 `tokens.css / animations.css / glow.css / scrollbar.css`。

---

## 4. 页面结构

### 路由（src/app/）

| 路径 | 行数 | 模式 | 说明 |
|------|------|------|------|
| `app/page.tsx` | 177 | **Client** (`'use client'`) | 首页 = 工作台入口，含未登录倒计时拦截 + `dynamic(WorkspacePanel, { ssr: false })` |
| `app/login/page.tsx` | 722 | **Client** | 登录页 |
| `app/profile/page.tsx` | 889 | **Client** | 个人中心 |
| `app/gallery/page.tsx` | 625 | **Client** | 作品画廊 |
| `app/admin/page.tsx` | **1,704** | **Client** | 后台管理（巨文件，疑似单页应用） |
| `app/layout.tsx` | 74 | **Server** | 根布局（RSC 模式：metadata, viewport, Providers 嵌套） |

### 关键数据点
- **`.tsx` 文件总数 91**，其中 **75 个 `'use client'`**——**82% 客户端组件**。这是项目最大的性能/SEO 风险。
- **无** `(marketing)` / `(studio)` / `(admin)` **路由组**——所有页面平铺在 `app/` 下。
- **无** `app/not-found.tsx` / `app/error.tsx` / `app/loading.tsx`。
- **无** Server Action（`grep 'use server' 0 命中`）。
- **无** `useActionState` / `useFormState` / `useFormStatus`（仅 `form.tsx` 提到 2 次，但 grep 实际是 shadcn form 内 ref 转发用法）。
- 唯一的 `next/dynamic` 使用：`src/app/page.tsx:14` 懒加载 WorkspacePanel；WorkspacePanel 内部用 `React.lazy` 懒加载 17 个功能面板。

### 证据
- `app/page.tsx:1` `'use client'`
- `app/admin/page.tsx:1` `'use client'`
- `wc -l` 实测各页面行数
- `grep -rln '^use server' src/app` → 0 命中
- `find src -name '*.tsx' | wc -l` → 91；`grep -rl '^use client' src | wc -l` → 75

### 缺口评级
- **高**：75/91 = 82% Client Components，**RSC 优势被完全放弃**。
- **高**：admin 单文件 1,704 行（无内部分包/路由分段）—— 维护与首屏体积都差。
- **高**：无 error.tsx / not-found.tsx / loading.tsx——错误边界缺失。
- **中**：无 Server Actions，所有数据交互走 `fetch('/api/...')` Route Handlers（`/api/*/route.ts` 共 ~50 个），RSC 协同、数据缓存、revalidate 全部用不上。

---

## 5. 状态管理

### 现状
- **Zustand / Redux / Jotai / Recoil / Valtio**：**0 命中**。
- **TanStack Query / SWR**：**0 命中**。
- **数据获取方式**：纯 `useEffect + fetch`，每个 workspace 组件内独立拉数据（见 `useGenerationHistory.ts` / `useImageUpload.ts` 等 13 个 hook）。
- **AuthProvider**：`src/providers/AuthProvider.tsx`（基于 React Context + 自写 `useAuth` hook）—— 唯一全局状态。
- **生成任务管理**：`useGenerationTaskManager.tsx`（Context）—— 唯一的跨组件任务编排。
- **表单**：
  - `react-hook-form 7.70.0` 已装，**仅 `components/ui/form.tsx` 1 处使用**（shadcn 模板代码，业务表单 0 实际引用）。
  - `@hookform/resolvers 5.2.2` 已装，**业务表单 0 实际使用**。
  - **所有登录/注册/充值/管理表单均为手写 `useState` + `onChange`**。

### 证据
- `grep "zustand|jotai|redux|@tanstack/react-query|swr" src` → 0 命中
- `useAuth.ts` / `useGenerationTaskManager.tsx` / `useGenerationHistory.ts` 等 13 个 hook 文件
- `components/ui/form.tsx:2` `import { useFormContext, Controller } from 'react-hook-form'`

### 缺口评级
- **高**：所有列表/历史/数据请求组件**重复实现 useEffect + fetch + loading + error** 模板，未做缓存/去重/乐观更新/失效策略；每个面板挂载都重新拉取历史，**用户体验差 + 网络浪费**。
- **中**：表单未用 RHF+zod，**类型安全与开发效率受损**。

---

## 6. 性能现状

### 现状

| 维度 | 是否使用 | 证据 |
|------|----------|------|
| `next/image` 组件 | **❌ 0 命中** | `grep "Image from 'next/image'"` → 0；全部用原生 `<img>`（26 个文件含 `<img` 或 next/image 字符串） |
| `next/font` | **❌ 0 命中** | `grep next/font` → 0；字体声明在 `globals.css` 用 `font-family: 'PingFang SC', ...`（系统字体堆栈，无自托管） |
| Bundle 分析 | **❌ 无** | package.json 中无 `@next/bundle-analyzer`，无 `ANALYZE=true` 脚本 |
| `dynamic()` 懒加载 | **仅 1 处** | `app/page.tsx:14` 懒加载 WorkspacePanel |
| `React.lazy + Suspense` | **WorkspacePanel 内 17 处** | `WorkspacePanel.tsx:8-24` 全部功能面板 |
| `Suspense` | **3 处** | WorkspacePanel + 上层 1 处 |
| `useTransition` / `useDeferredValue` | **0 命中** | 路由/搜索输入全同步 |
| `React.memo` / `useMemo` 优化 | **0 命中**（grep "memo(" 0 命中） | 全部子组件随父级 re-render |
| Streaming (`loading.tsx`) | **❌ 无** | 无 `app/loading.tsx` |
| 图片优化 | 走 `next.config.ts images.remotePatterns`（已配置）但实际**未使用 next/image**，远端图片无法被自动转 AVIF/WebP | `next.config.ts:63-71` |
| CORS / 安全头 | **完整** | `next.config.ts:11-61`：CORS + XCTO + XFO + XXSS + Referrer + Permissions |
| `serverExternalPackages` | `bcryptjs`, `pg` | `next.config.ts:74` |

### 证据
- `next.config.ts:63-71` images.remotePatterns
- `WorkspacePanel.tsx:8-24` 17 个 `lazy(() => import(...))`
- `grep "next/image" src` → 0
- `grep "next/font" src` → 0
- `grep "useTransition\|useDeferredValue\|memo(" src` → 0

### 缺口评级
- **高**：
  1. 远端图片用 `<img>` 直接加载，**首屏 LCP 与带宽无优化**。
  2. 无 bundle 分析，**无法判断 17 个 lazy chunk 实际尺寸**。
  3. 75/91 Client Components，首屏 JS 包估算 **500KB+ gzip**。
- **中**：
  1. 无 `useTransition` 路由切换直接用 `router.push`（`page.tsx:65`）。
  2. 无 memoization，17 个 lazy 组件无 `startTransition` 包裹，**会触发 fallback 闪烁**。

---

## 7. 可访问性 (a11y)

### 现状

| 维度 | 状态 | 证据 |
|------|------|------|
| Radix UI 基础 | **完整** | 20 个 `@radix-ui/react-*` 依赖 + 19 个对应的 shadcn UI 包装（dialog/select/popover/tabs/...）—— **a11y 基底层到位** |
| 自定义 ARIA | **极弱** | 整个 src 下 `aria-*=` 出现 **9 次**（13 个文件） |
| `role` 属性 | **1 次** | `WorkspacePanel.tsx:94` `<Image role="img" aria-label="功能模块">` |
| 键盘事件 / 焦点管理 | **4 个文件** | 主要是 AIDialog 输入框 + admin 表格行 |
| 跳转链接 / skip-to-content | **0 命中** | 无 `<a href="#main">` / `skip-link` |
| 焦点环 | 部分使用 `.focus-visible:ring-2`（shadcn 默认）但自定义金主题后**`--ring: var(--gold)`，对比度需测**| `globals.css:138` |
| Sonner Toast | 通过 next-themes 自动 dark，**a11y 由 sonner 内置** | `sonner.tsx:1-10` |
| 错误边界 | **0** | 无 `app/error.tsx` |
| 语义化 | 部分使用 `<button>` 替代 `<div onClick>`，但 admin 1,704 行文件中存在**大量 `<div onClick>` 反模式风险** | 需逐文件审查 |

### 证据
- `package.json:27-52` 20 个 `@radix-ui/*` 依赖
- `grep -rE "aria-[a-z]+=" src` → 9 次
- `grep "rovingTabindex|onKeyDown|tabIndex" src` → 4 文件

### 缺口评级
- **中**：Radix 给的 a11y 基础完整，但**业务层 ARIA 严重不足**——AI 写作助手、悬浮工具栏、AI 对话流、画廊瀑布流、admin 表格**几乎没有 ARIA 标注**。
- **中**：错误边界缺失，**单组件抛错即整页白屏**。
- **低**：sonner + next-themes 已处理 Toast a11y。

---

## 8. i18n / PWA

### 现状

| 维度 | 是否集成 | 证据 |
|------|----------|------|
| `next-intl` / `next-i18next` | **❌** | 0 命中（package.json 无此依赖） |
| `i18n/` / `messages/` 目录 | **❌** | `ls` 实测两目录都不存在 |
| 中英文文案策略 | **完全硬编码中文** | 所有页面 `.tsx` 内字符串字面量均为中文（如 `WorkspacePanel.tsx:52-70` 的 `panelTitles`） |
| PWA Manifest | **❌** | `grep manifest.webmanifest` → 0；`grep "manifest" layout.tsx` → 0（无 `<link rel="manifest">`） |
| Service Worker | **❌** | `sw.js` 0 命中；无 `next-pwa` / `@serwist/next` 依赖 |
| `<html lang>` | **固定 `zh-CN`** | `layout.tsx:59` |
| OG / SEO | **部分** | layout.tsx 配置了 metadata.title/description/keywords/openGraph/robots，但**无 twitter card、无 canonical、无 JSON-LD** |
| 离线降级 | **❌** | 无缓存策略、无离线页 |
| Open Graph 图 | **❌** | 无 `app/opengraph-image.tsx` / `public/og.png` |

### 证据
- `package.json` 全文无 `next-intl` / `next-i18next` / `i18next` / `@serwist/next` / `next-pwa`
- `ls src/i18n` / `ls messages` → 目录不存在
- `layout.tsx:12-42` metadata 配置 / `layout.tsx:59` `lang="zh-CN"`

### 缺口评级
- **高**：
  1. **完全无 i18n 框架**——所有 50,000+ 字中文文案散落 91 个 .tsx 文件，国际化将需要**全文重写**。
  2. **无 PWA**——AI 工具类应用适合 PWA（断网排队/后台通知/桌面安装），但完全未实现。
- **中**：
  1. SEO 配置**只到 metadata 基础层**，无 OG 图、无结构化数据，社交分享体验差。

---

## 综合评级总览

| 类别 | 评级 | 关键风险 |
|------|------|----------|
| 技术栈版本 | 中 | docs 错位（AGENTS.md 写 Next 16，实际 15.1.0）；eslint-config-next 16 与 Next 15 错配 |
| UI 组件库 | 小 | shadcn 33 个 + workspace 17 + 8 个自定义，**功能覆盖完整** |
| 主题系统 | 中 | next-themes 装了但只用于 sonner；硬编码 dark；globals.css 1,224 行偏胖 |
| 页面结构 | **高** | 75/91 Client Components；admin 单文件 1,704 行；无 error/not-found/loading.tsx；无 Server Actions |
| 状态管理 | **高** | 零 Zustand/Query；手写 useEffect+fetch；RHF 已装但业务表单 0 使用 |
| 性能 | **高** | 无 next/image、next/font、bundle 分析；远端图直接 `<img>` 加载；无 memo/transition |
| a11y | 中 | Radix 基底完整但业务 ARIA 9 处；无错误边界；键盘导航不足 |
| i18n / PWA | **高** | 完全无 i18n 框架；完全无 PWA；全中文硬编码 |

### 三大优先改造项（建议）
1. **引入 TanStack Query + 拆分 admin 页面**（先解决 N+1 fetch 重复 + 1704 行单文件风险）。
2. **启用 next/image + next/font + bundle analyzer**（解决远端图优化与首屏 JS 包大小黑盒）。
3. **改造 5-10 个核心页为 RSC + Server Action**（将 Client Component 比例从 82% 降到 50% 以下，释放 RSC 流式渲染与缓存优势）。
