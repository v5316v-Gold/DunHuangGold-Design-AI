# 📋 敦煌金 AI 设计平台 — 基线评估报告

> **生成时间**：2026-07-28
> **扫描方式**：5 维度并行静态扫描（安全 / 数据库 / AI / 前端 / 部署）
> **未修改任何代码**，仅评估现状与差距
> **目标读者**：项目所有者 / 后续改造者

---

## 🏛️ 总体评分

| 维度 | 现状评分 | 目标（生产就绪） | 差距 |
|------|:---:|:---:|:---:|
| **安全 / 鉴权** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 大 |
| **数据库** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 大 |
| **AI 网关 / 功能** | ⭐⭐ | ⭐⭐⭐⭐⭐ | **巨大** |
| **前端 / UI** | ⭐⭐⭐ | ⭐⭐⭐⭐ | 中 |
| **部署 / 运维** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 中 |
| **总体** | ⭐⭐⭐☆☆ | ⭐⭐⭐⭐⭐ | **不可商用，需重点改造** |

---

## 🔴 一、致命问题（生产事故级，必须立刻修）

### 1️⃣ **算力扣减可被绕过 / 可双花 / 无退款**

**位置**：
- `src/lib/ai-service/power-helper.ts:41-87`（`deductUserPower`）
- `src/app/api/power/route.ts:93-141`

**问题**：
```ts
// 现状：read-then-write，无事务，无行锁
const [user] = await db.select().from(users).where(eq(users.id, userId)).limit(1)
if (user.power < cost) return { error: '算力不足' }
const newPower = user.power - cost
await db.update(users).set({ power: newPower }).where(eq(users.id, userId))
await db.insert(powerLogs).values({ balance: newPower, ... })
```

**风险**：
- ❌ **并发扣减导致透支**：A、B 同时扣 30（初始 50），都过 check，都写 20，最终 20（应是 -10）
- ❌ **失败无退款**：pipeline 失败不退还算力
- ❌ **算力单价在前端 localStorage**：用户可改成 1 → 全平台免费
- ❌ **无幂等性**：重复点击扣多次

**修复方案**（基于 `drizzle-orm-patterns` + `ai-cost-power-metering` skill）：
```ts
// 必须改为：事务 + SELECT FOR UPDATE + 行锁
return db.transaction(async (tx) => {
  const [user] = await tx.select().from(users)
    .where(eq(users.id, userId))
    .for('update')  // 行锁
  
  if (user.power < cost) throw new InsufficientCreditsError(cost, user.power)
  
  const [updated] = await tx.update(users)
    .set({ power: sql`${users.power} - ${cost}` })
    .where(eq(users.id, userId))
    .returning({ power: users.power })
  
  await tx.insert(powerLogs).values({
    userId, type: 'consume', amount: -cost, balance: updated.power, ...
  })
})
```

**关键**：
- 单价必须从后端取，不从前端
- 失败必须 refund（try/catch + 退还）
- 加幂等性（UNIQUE 约束）

---

### 2️⃣ **JWT 写入 localStorage（XSS 可窃取）**

**位置**：`src/hooks/useAuth.ts:78-105`

**问题**：JWT 同时存入 localStorage，HttpOnly cookie 也并存。XSS 攻击可直接 `localStorage.getItem('token')` 拿到 7 天有效 token。

**修复**：
- ❌ 移除 localStorage 存储
- ✅ 仅用 HttpOnly + Secure + SameSite=Lax cookie
- ✅ 引入 Refresh Token 机制（短 access 15min + 长 refresh 7d）

---

### 3️⃣ **两套并行数据库 Schema（数据一致性崩溃）**

**位置**：
- `src/db/schema.ts` + `src/db/schema/_tables.ts`（legacy）
- `src/storage/database/shared/schema.ts`（新版）

**问题**：
- 两个 schema 互不兼容（`users` 字段不同；`power_logs` vs `power_records`；`works` vs `artworks`）
- 致命 bug：`src/lib/ai-service/power-helper.ts:6-7` 跨 schema 导入
  ```ts
  import { db } from '@/storage/database/db'          // ← storage 连接
  import { users, powerLogs } from '@/db/schema'     // ← legacy 表对象
  ```
  db 实例来自 storage schema，但表对象来自 legacy schema。**AI 生成链路运行时崩溃或数据错乱。**

**修复**：
1. **合并到单一 schema**：统一用 storage schema（已带 drizzle-kit 版本化迁移）
2. **修复 power-helper.ts**：确保 db 实例与表对象来自同一 schema
3. **迁移所有 route.ts**：从 `@/db/schema` 改为 `@/storage/database/shared/schema`
4. **删除 legacy schema 文件**：`src/db/schema.ts`、`src/db/schema/_tables.ts`、`src/db/schema/power-transactions.ts`

---

### 4️⃣ **任务队列：表定义了但完全没使用**

**位置**：`src/db/schema/_tables.ts:83-96`（`tasks` 表定义）

**问题**：
- ✅ `tasks` 表定义了 `pending/processing/completed/failed` 状态
- ❌ **没有任何代码写入**（grep `tasks` 写入 = 0）
- ❌ **没有 Worker 进程**（无 BullMQ、无 cron）
- ❌ AI 生成是**同步阻塞**：ComfyUI 几分钟生成期间请求线程卡死

**修复**：
- 引入 BullMQ 队列（Redis 后端）
- Worker 进程独立运行
- 任务状态机：`queued → processing → completed | failed | cancelled`
- 前端用 SSE 推送进度

---

### 5️⃣ **14 / 17 个 AI 功能是空壳 UI**

**位置**：`src/components/workspace/` 下 17 个组件

**问题**：
- ✅ `Text2Image.tsx` + `ProductRefine.tsx` + `ReliefDesign.tsx` = **真实现**
- ❌ `Image3D.tsx`、`MultiImage.tsx`、`MultiView.tsx`、`OneClickDesign.tsx`、`SketchRealistic.tsx`、`FreeCreation.tsx`、`RemoveBackground.tsx`、`Upscale.tsx`、`RemoveWatermark.tsx`、`TryOnEffect.tsx` = **零 fetch 调用**，纯占位 UI
- `service-registry` 只注册 3 个服务

**修复**：基于 `ai-gateway-multi-provider` skill 落地 14 个 AI Provider 实现。

---

## 🟡 二、重要问题（P1，业务上线前）

### 安全 / 鉴权

| 缺口 | 评级 | 修复 |
|------|------|------|
| 无 Refresh Token / Token Rotation | P1 | 加 15min access + 7d refresh + rotation |
| 15+ 个 admin route 散写 `role !== 'admin'` | P1 | 抽 `requireAdmin` / `requireOwnership` 工具 |
| 无登录失败计数 / 账户锁定 | P1 | 加 brute force 防护 |
| 密码策略仅 `>=6` | P1 | 加复杂度要求 |
| `cookie.secure = false` 硬编码 | P1 | 根据 NODE_ENV 动态设 |
| `/admin/page.tsx` 客户端判定 | P1 | server-side redirect |
| 无专门 `audit_logs` 表 | P1 | 建表 + 写所有管理员操作 |
| `api_configs.api_key` 明文 | P1 | 字段级加密（pgcrypto） |

### 数据库

| 缺口 | 评级 | 修复 |
|------|------|------|
| 零 GIN / 复合索引 | P1 | 核心查询加复合 + JSONB GIN |
| 无软删除（deletedAt） | P1 | 所有业务表加 deletedAt + 索引 |
| `ssl: false` 数据库连接 | P1 | 生产加 `?sslmode=require` |
| 无 PgBouncer | P1 | 多实例部署前必加 |
| 缺失 `topup_records` / `generation_jobs` / `lora_models` / `comments` | P1 | 建表 |
| 无 RLS | P1 | 多租户隔离 |

### AI / Provider

| 缺口 | 评级 | 修复 |
|------|------|------|
| 算力扣减（前述致命 #1） | P0 | 事务 + 行锁 |
| 14 个 AI 功能空壳 | P1 | 落地 Provider 实现 |
| 工作流 JSON 硬编码在代码里 | P1 | 抽到 `src/lib/workflows/*.json` + LoRA 挂载逻辑 |
| Failover 仅 1 个真实现（minimax） | P1 | 完善 Provider 抽象层 |
| 无 Provider 健康检查 Worker | P1 | 加定时巡检 + 自动 down 标记 |
| 无退款逻辑 | P1 | pipeline 失败自动退还 |

### 前端

| 缺口 | 评级 | 修复 |
|------|------|------|
| 82% Client Component（91 个 tsx 中 75 个 `'use client'`） | P1 | 核心页面改 RSC + Server Action |
| admin 单文件 1704 行 | P1 | 拆分为模块 |
| 0 Server Actions / 0 useActionState | P1 | 表单改 Server Action |
| 0 next/image（26 处用 `<img>`） | P1 | 改 next/image |
| 0 next/font（系统字体堆栈） | P2 | 接 next/font 子集化 |
| 0 Zustand / TanStack Query | P1 | 引入数据状态层 |
| 0 i18n / 0 PWA | P2 | 加 next-intl + PWA manifest |

### 部署 / 运维

| 缺口 | 评级 | 修复 |
|------|------|------|
| 生产 compose 缺 db/redis 容器 | P1 | 提供完整 compose |
| 无 `/api/metrics` Prometheus | P1 | 引入 prom-client |
| 无 Sentry | P1 | 引入 @sentry/nextjs |
| 无自动备份 | P1 | 1Panel 计划任务 + cron + OSS |
| `proxy_buffering off` 缺 | P1 | 生产 conf 加上 |
| 无自动部署 CI | P2 | 加 deploy Job |
| Healthcheck 路径不一致 | P3 | Dockerfile 用 `/api/health` |
| `instances: 1` 未集群 | P3 | cluster 模式 |

---

## 🟢 三、可选问题（P2，产品成熟期）

- MFA / 2FA
- 密码历史 / 复杂度规则
- API Key 字段级加密（pgcrypto）
- 集中化日志栈（Loki / Datadog）
- Dependabot 自动依赖审计
- OpenTelemetry 全链路追踪
- 蓝绿部署 + 自动回滚
- ESLint / Next 15 / eslint-config-next 版本对齐
- AGENTS.md 文档与实际版本对齐（Next 15 ≠ 16）
- OG 图 / 结构化数据 / canonical SEO

---

## 📊 四、按严重度的改造优先级排序

### 🔴 立即修（P0 致命，1-2 周）

| 序 | 任务 | 文件 | 优先级 |
|---|------|------|:---:|
| 1 | **算力扣减改事务 + 行锁 + 退款** | `lib/ai-service/power-helper.ts`, `app/api/power/route.ts` | 🔥🔥🔥 |
| 2 | **JWT 移除 localStorage** | `hooks/useAuth.ts`, `app/api/auth/login/route.ts` | 🔥🔥🔥 |
| 3 | **合并双 schema** | `lib/ai-service/`, 所有 route | 🔥🔥🔥 |
| 4 | **算力单价强制后端** | `lib/feature-costs.ts`（前端不存单价） | 🔥🔥🔥 |
| 5 | **任务队列 + Worker** | 新建 `lib/queue/` + `workers/` | 🔥🔥 |
| 6 | **限流加固 + 防暴力破解** | `lib/rate-limit.ts` 已有，再加 IP 封禁 | 🔥🔥 |
| 7 | **`audit_logs` 表 + 管理员操作记录** | 新建 schema | 🔥🔥 |

### 🟡 半年内（P1 业务上线前，4-8 周）

| 序 | 任务 | 涉及文件 |
|---|------|---------|
| 8 | Refresh Token + rotation | `lib/auth.ts` + 新表 |
| 9 | 抽 `requireAdmin` / `requireOwnership` | `lib/auth/guards.ts` |
| 10 | 14 个 AI 功能落地 | `components/workspace/*` |
| 11 | 前端 RSC 改造（拆 admin 1704 行单文件） | `app/admin/page.tsx` |
| 12 | 引入 TanStack Query | `package.json` |
| 13 | 引入 next/image / next/font | 全局 |
| 14 | Prometheus + Sentry | `app/api/metrics/`, `sentry.*.config.ts` |
| 15 | nginx `proxy_buffering off` | `deploy/nginx.conf` |
| 16 | 软删除 deletedAt + 索引 | 所有业务表 |
| 17 | 自动备份（1Panel 计划任务 + OSS 异地） | 1Panel 配置 |
| 18 | 密码策略 / 账户锁定 | `lib/auth.ts` |
| 19 | api_configs.api_key 加密 | migration + 应用层 |
| 20 | 失败退款逻辑 | `lib/ai-service/generation-pipeline.ts` |

### 🟢 长期（P2 一年持续改进）

| 序 | 任务 |
|---|------|
| 21 | next-intl 国际化 |
| 22 | PWA 离线 |
| 23 | OpenTelemetry 全链路追踪 |
| 24 | 蓝绿部署 + 自动回滚 |
| 25 | OpenAPI 3.1 自动生成文档 |
| 26 | i18n + SEO + OG 图 |
| 27 | Dependabot 自动依赖审计 |
| 28 | 性能优化（Lighthouse ≥ 95） |
| 29 | 微服务拆分（按 Bounded Context） |
| 30 | 完整 Docker Hub / GHCR + 蓝绿 |

---

## 📐 五、改造架构图（建议）

```
改造前（现状）：                         改造后（目标）：
                                        
┌──────────────────┐              ┌──────────────────┐
│  Frontend (82% C) │              │  RSC + Server A. │
└────────┬─────────┘              └────────┬─────────┘
         │                                 │
         │ JWT in localStorage              │ HttpOnly cookie
         │                                 │
         ▼                                 ▼
┌──────────────────┐              ┌──────────────────┐
│  Next.js Route   │              │  Next.js Route   │
│  Handler (15+)   │              │  Handler + DAL   │
└────────┬─────────┘              └────────┬─────────┘
         │                                 │
         │ 散写 admin check                 │ requireAdmin()
         │                                 │ requireOwnership()
         ▼                                 ▼
┌──────────────────┐              ┌──────────────────┐
│  Sync AI call    │              │  BullMQ Queue    │
│  (阻塞)          │              │  + Worker        │
└────────┬─────────┘              └────────┬─────────┘
         │                                 │
         │ 无 fallback                      │ Fallback chain
         │                                 │ Provider 抽象
         ▼                                 ▼
┌──────────────────┐              ┌──────────────────┐
│  read-then-write │              │  Tx + FOR UPDATE │
│  无事务 无锁      │              │  + 退款 + 幂等   │
└──────────────────┘              └──────────────────┘
```

---

## 🎯 六、建议的执行顺序（3 个迭代 Sprint）

### Sprint 1（2 周）— 安全止血
**目标**：把致命 #1-#4 全部修完

- ✅ 算力扣减改事务 + 行锁 + 退款
- ✅ JWT 移除 localStorage + Refresh Token
- ✅ 合并双 schema
- ✅ 算力单价强制后端
- ✅ 限流加固（IP 封禁 + 失败计数）
- ✅ audit_logs 表 + 关键操作记录

**验收标准**：
- 并发 10 扣减同一用户，余额准确
- XSS 测试无法获取 token
- AI 生成失败自动退款
- 管理员改算力有审计日志

### Sprint 2（4 周）— AI 网关 + 任务队列
**目标**：AI 平台骨架可商用

- ✅ BullMQ 队列 + Worker 进程
- ✅ Provider 抽象层 + 14 个 AI 功能落地
- ✅ 工作流 JSON 外置 + LoRA 挂载
- ✅ 健康检查 Worker
- ✅ 失败退款 + 幂等性

**验收标准**：
- 17 个 AI 功能全部可调用
- 任务异步执行（前端立即返回 jobId）
- SSE 推送实时进度
- ComfyUI 宕机自动 fallback

### Sprint 3（6 周）— 前端 + 部署 + 可观测
**目标**：生产就绪

- ✅ 前端 RSC 化（拆 admin + 引入 TanStack Query）
- ✅ next/image / next/font / bundle 分析
- ✅ Sentry + Prometheus + OpenTelemetry
- ✅ nginx 优化（buffering / 限流 / keepalive）
- ✅ 自动备份 + 异地 OSS
- ✅ 蓝绿部署 + CI/CD

**验收标准**：
- Lighthouse ≥ 90
- 线上错误 5 分钟内 Sentry 报警
- 数据库每日自动备份
- 部署无停机

---

## 📦 七、文档现状 vs 待补

### 已有的（不需重写）
- ✅ `1PANEL-DEPLOYMENT-GUIDE.md`（765 行，较完整）
- ✅ `DEPLOYMENT.md`（233 行）
- ✅ `LAN_DEPLOYMENT.md`（293 行）
- ✅ `LOCAL_AI_SETUP.md`
- ✅ `REFACTOR_*.md`（4 份重构指南）

### 需新增
- ❌ **API 接口文档**（OpenAPI 3.1）
- ❌ **架构图**（五层架构 + 数据流）
- ❌ **安全策略文档**（密码、限流、审计）
- ❌ **运维手册**（告警、备份、灾备演练）
- ❌ **开发者手册**（本地开发 / 测试 / 调试）
- ❌ **CHANGELOG.md**（变更日志）

---

## 🚀 八、总结

| 类别 | 现状 | 关键问题 | 建议优先级 |
|------|------|---------|----------|
| **安全** | 中 | JWT localStorage / 算力无锁 / 无审计 | 🔴 P0 |
| **数据库** | 中 | 双 schema 并存 / 无事务 / 无高级索引 | 🔴 P0 |
| **AI 网关** | **差** | 14/17 是空壳 / 同步阻塞 / 无队列 | 🔴 P0 |
| **前端** | 中 | 82% Client / 单文件 1704 行 / 无 i18n | 🟡 P1 |
| **部署** | 中 | 无 Prometheus / 无 Sentry / 无自动备份 | 🟡 P1 |

**一句话总结**：
> 项目是一个**有完整 UI、健全的 Provider 注册机制雏形、健全的安全基础**的应用，但**算力扣减、任务队列、14 个 AI 功能**仍处于**骨架阶段**，**算力扣减还有可被绕过 / 可双花的安全性漏洞**，距离"production ready"还需 3 个 Sprint（约 12 周）的迭代。

---

**报告完。下一步建议**：
1. 立即启动 **Sprint 1（安全止血）**
2. 我可以立刻帮你开始第 1 项：算力扣减改事务 + 行锁 + 退款（详细方案 + 代码）
3. 或者你可以从 P0 列表中选择先做哪一项

请告诉我你希望先从哪个任务开始？🚀